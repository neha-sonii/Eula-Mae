{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "install:site": "npm install --prefix site",
    "dev": "npm run dev --prefix site",
    "build": "npm run build --prefix site",
    "serve": "npm run serve --prefix site"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
